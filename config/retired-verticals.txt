# Vertical ids deliberately removed from this repo.
#
# app/{routes,public,services}/verticals and config/customers are additive on
# the demo host (see scripts/deploy-ec2.sh), so deleting a vertical here leaves
# it serving in production forever. Listing its id here deletes those four
# files on the next deploy. One id per line; blank lines and # comments ignored.
#
# Only list ids this repo owns — an id the other source repo still ships would
# be deleted out from under it on every deploy.

# Al Rajhi sign-in: replaced by the per-owner slug d7c4a1b9, which is reachable
# only at /d7c4a1b9/<owner>. The shared /login URL put one person's demo in
# front of the whole company.
login
