node_modules/
.env
*.log
.git/
.gitignore
README.md
.dockerignore
coverage/
.DS_Store
certbot/
nginx/*-rendered.conf
mobile/
# Host-only paths on the EC2 box (the build context there is the live
# /home/ubuntu tree). incoming/ and release-*.tar.gz are written by a
# concurrently queued deploy from the other repo while this one builds.
incoming/
releases/
release-*.tar.gz
archive/
avature-src/
*.tgz
*.tar.gz
*.bak
*.bak.*
